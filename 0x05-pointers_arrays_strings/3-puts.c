#include "main.h"
/**
 * _put - prints a string to stdout, followed by a new line.
 * @str: pointer to the string to be printed
 * Return: void
 */
void _puts(char *s)
{
	for (; *str != '\0'; str++)
	{
		putchar(*str);
	}

	putchar('\n');
}
